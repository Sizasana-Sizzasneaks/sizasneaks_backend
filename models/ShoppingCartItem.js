const shoppingCartItem = {
    product_id: String,
    quantity: Number,

    Option: {
        color:String,
        size: Number,
    },
    imgURLs: String,
    price: Number
}

module.exports = shoppingCartItem;